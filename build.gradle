import com.sun.tools.javac.Main

plugins{
    id 'application'    // Implicitly applies the java and distribution plugins
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

mainClassName='com.coin.price.checker.CoinPrice'
version=1.0
